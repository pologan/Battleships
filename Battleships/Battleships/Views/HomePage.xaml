<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Battleships.Views.HomePage"
             xmlns:vm="clr-namespace:Battleships.ViewModels">
    <ContentPage.Content>
        <StackLayout x:DataType="vm:HomeViewModel">
            <Label Style="{StaticResource Header}" Text="Battleship Simulator"
                VerticalOptions="Start" 
                HorizontalOptions="CenterAndExpand" />

            <Button Text="Place ships"
                    VerticalOptions="CenterAndExpand"
                    HorizontalOptions="CenterAndExpand" 
                    Command="{Binding PlaceShipsCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>